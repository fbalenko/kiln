// Default state for the @modal parallel slot — renders nothing when
// no intercepting route has matched. The slot is required by the
// parallel-routes contract.
export default function Default() {
  return null;
}
